<%= turbo_stream.replace dom_id(@response.question, :response_form_frame) do %>
  <turbo_frame id=<%= dom_id(@response.question, :response_form_frame) %>>
    <%= render "responses/form", question: @response.question, response: @response  %>
  </turbo_frame>
<% end %>
